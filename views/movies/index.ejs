<% include ../partials/header.ejs %>

<div class="container">
    <header class="jumbotron">
        <div>
            <h1>Welcome to Rotten Potatoes!</h1>
            <hr>
            <p>View our trusted movie review from all of the world</p>
        </div>
    </header>
    <div class="row">
        <% movies.forEach(function (value) { %>
        <div class="col-md-3 col-sm-6">
            <div class="thumbnail" id="index-header">
                <img src="<%= value.poster %>" alt="">
                <div class="caption">
                    <strong><h4><%= value.title %>  (<%= value.year %>)</h4></strong>
                </div>
                <p><a href="/movies/<%= value.id %>" class="btn btn-primary">More Info</a></p>
            </div>
        </div>
        <% }) %>
    </div>
</div>

<% include ../partials/footer.ejs %>